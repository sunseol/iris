# MEMORY.md

## 2026-03-12
- Preference: when sharing Expo/app access links for testing, default to externally reachable addresses — prefer Expo tunnel or Tailscale-accessible addresses over LAN-only local IPs whenever possible.
- Preference: assume external accessibility is the default for this kind of testing/setup work unless the user says otherwise.

## 2026-03-10
- Preference: keep using GPT 5.4 as the model going forward when possible.

## 2026-02-27
- User correction to remember: Vercel is **not only** about AI model gateway use-cases. It also includes broader platform capabilities (deployment, previews, functions/compute, AI SDK/agents workflows, marketplace/integrations, etc.).
- Preference: avoid oversimplifying Vercel as just "model gateway" when giving guidance.
- User asked to also track "Vercel skills" options; relevant ClawHub candidates include `vercel` (Vercel Platform) and `vercel-deploy` (deployment-focused operations).
