Tech stack: TypeScript + Expo React Native in `app/`; Node.js ESM JavaScript in `bridge/`. App uses `tsc --noEmit` as lint/type gate and Node's test runner via `tsx --test`. Bridge uses plain JS modules, `node --check` for lint/build validation, and `node --test` for tests. Code style appears minimal, straightforward, and comment-light. Root scripts are the canonical way to run repo-wide checks.