# 2026-03-12

- Continued major UI/product restructuring of `opencode-mobile/app` away from a generic chat app and toward a project-first, thread-centric mobile coding agent workspace.
- Landed project-first state usage in the UI: sessions are presented as project-scoped threads; active session restoration remains per-project via `activeSessionIdByProject`.
- Added/expanded dedicated workspace UI components and screens over several turns: `ProjectWorkspaceScreen`, `ProjectsScreen`, `ModelsAuthScreen`, `ConnectionScreen`, plus supporting rail/runtime/transcript/composer components.
- Verified a runtime/session status semantics refactor is landed in current code: shared `ProjectSession.status` semantics now explicitly include `idle`, `running`, `waiting_approval`, `cancelled`, `disconnected`, and `error`; `cancelled` is no longer silently collapsed into `idle` in the app UI state flow.
- Confirmed `ProjectWorkspaceScreen`, `RuntimeStateCard`, and thread/session rail components are reading the same `ProjectSession.status` source rather than inventing separate status interpretations.
- Introduced workbench-style visual direction in the Expo app: dense dark UI, technical status chrome, thread cards, runtime panel, structured diff review card, and command-bar-style composer.
- Reworked thread/session navigation from an inline list into a mobile sheet-based thread navigator: modal bottom sheet, backdrop dismiss, swipe-down close, project-scoped thread cards, and preserved active-thread context in the main workspace when the rail is closed.
- Repeated user requirement to remember: do not claim a fix/work item is landed after an `edit failed` event without re-checking actual file contents and showing current-code evidence or diff first.
- Repeated user completion rule to remember for `opencode-mobile`: do not declare completion from happy-path success alone; completion requires unresolved P0/P1 blocker review and explicit evidence.
- Validation status at latest checkpoint: `opencode-mobile` lint/test remained green after the workspace/rail changes (`app` tests 7/7 pass, `bridge` tests 13/13 pass).
- Git remote `sunseol/iris` was corrected from accidentally pushing the whole workspace to containing only the `opencode-mobile` project; the remote was force-updated after excluding generated/build artifacts.
- User preference reinforced during Expo/testing work: when reopening the mobile app server, start it in Expo tunnel mode and include the Expo tunnel URL again in the reply.
- Current UI work on `opencode-mobile/app` has been pushed further toward the Irisfront reference, but as of the latest screenshot-based review the user still considers it only partial progress; do not report completion unless the reference layout match is visibly close in structure, density, top bar, workspace proportions, composer, and sheet/list treatment.
