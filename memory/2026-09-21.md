# 2026-09-21

## Heartbeat: system check (04:59 KST)
- Gateway healthy: systemd active (pid 1401), reachable 17ms, no events, no cron failure signs.
- Update available: openclaw 2026.9.5 (not urgent).
- Security audit flagged 2 CRITICAL (standing config issues, not incidents):
  1. `tools.elevated.allowFrom.telegram` contains `"*"` (wildcard) — effectively approves everyone on Telegram for elevated exec.
  2. `/home/jakeseol/.openclaw/credentials` mode=775 — writable by others. Fix: `chmod 700`.
- Deferred report to daytime heartbeat per night rule. TODO(next daytime heartbeat): surface these to user if still present; suggest exact fixes above.
