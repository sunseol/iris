# HEARTBEAT.md

목표: 하트비트가 올 때마다 필요한 점검을 **가볍게 1개씩** 수행하고, 사용자에게 가치가 있을 때만 알린다.

## 공통 원칙
- 아래 절차를 따른다. 과거 채팅 맥락을 억지로 추론하지 않는다.
- 야간(23:00~08:00, Asia/Seoul)에는 **긴급 상황이 아니면 알림하지 않고** `HEARTBEAT_OK`를 반환한다.
- 결과가 없거나 행동 필요가 없으면 `HEARTBEAT_OK`만 반환한다.
- 외부 전송/메시지 발송/삭제 같은 파괴적 작업은 하지 않는다.

## 실행 절차 (매 하트비트)
1. `memory/heartbeat-state.json` 파일을 읽는다. 없으면 생성한다.
2. 아래 체크 중 **가장 overdue 1개만** 실행한다.
3. 체크 결과가 actionable이면 간단히 보고한다.
4. 실행한 체크의 `lastChecks` 타임스탬프를 업데이트한다.
5. 보고할 게 없으면 `HEARTBEAT_OK`.

## 체크 목록 및 주기
- **tasks**: 30분마다 (항상)
  - 대상: 현재 워크스페이스 진행 작업 상태
  - 보고 조건:
    - 미완료 TODO/문서 작업 중 24시간 이상 정체 항목 발견
    - 즉시 결정이 필요한 블로커 발견
- **git**: 6시간마다 (08:00~23:00)
  - 대상: workspace git 상태
  - 보고 조건:
    - 커밋 안 된 변경사항 다수 누적
    - 충돌/에러 상태 감지
- **system**: 24시간마다 (03:00~05:00)
  - 대상: OpenClaw 상태/기본 헬스
  - 보고 조건:
    - gateway 비정상/에러 징후
    - 주기 작업 실패 징후

## 보고 형식
- 한글 3~6줄, 핵심만.
- 형식:
  - [체크명]
  - 발견사항
  - 필요한 다음 액션(있으면 1줄)

## 상태 파일 스키마 (`memory/heartbeat-state.json`)
```json
{
  "lastChecks": {
    "tasks": null,
    "git": null,
    "system": null
  }
}
```
